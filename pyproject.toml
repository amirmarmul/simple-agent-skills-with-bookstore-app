[project]
name = "simple-agent-skills-with-bookstore-app"
version = "0.1.0"
description = "FastAPI bookstore app and Agent Skills playground."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.0",
    "uvicorn>=0.45.0",
]
